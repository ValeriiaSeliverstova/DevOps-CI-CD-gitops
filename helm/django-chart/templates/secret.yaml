apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ required "Set secret.POSTGRES_PASSWORD or provide it via a separate values file." .Values.secret.POSTGRES_PASSWORD | quote }}
